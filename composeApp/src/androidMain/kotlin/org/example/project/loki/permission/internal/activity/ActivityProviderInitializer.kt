package org.example.project.loki.permission.internal.activity

import androidx.startup.Initializer

/**
 * Initializes the [ActivityProvider] during application startup.
 *
 * This initializer is responsible for creating and providing an instance of [ActivityProvider].
 * It utilizes the [Initializer] interface from the AndroidX Startup library to ensure that
 * the [ActivityProvider] is available early in the application's lifecycle, specifically
 * during the initialization phase.
 *
 */

/*
@InternalCompassApi
@SuppressLint
class ActivityProviderInitializer : Initializer<ActivityProvider> {
    override fun create(context: Context): ActivityProvider {
        return ActivityProvider.create(context)
    }
    override fun dependencies(): List<Class<out Initializer<*>>> = emptyList()
}*/
